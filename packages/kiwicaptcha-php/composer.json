{
    "name": "kiwicaptcha/kiwicaptcha-php",
    "description": "KiwiCaptcha: privacy-first, self-hosted proof-of-work anti-abuse engine for PHP. Zero third-party services or tracking; optional first-party telemetry; PHP-native verification (the optional RSW time-lock algorithm uses GMP).",
    "type": "library",
    "license": "MIT",
    "keywords": ["captcha", "proof-of-work", "sha256", "argon2", "security", "privacy"],
    "require": {
        "php": ">=8.1",
        "ext-json": "*",
        "ext-sodium": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5",
        "predis/predis": "^3.5",
        "psr/cache": "^3.0",
        "psr/log": "^3.0"
    },
    "suggest": {
        "ext-gmp": "Required only for the optional RSW time-lock algorithm",
        "ext-openssl": "Optional: the core hashes and HMACs use ext-sodium/ext-hash primitives; install only if other parts of your stack need it",
        "psr/cache": "Required for the PSR-6 challenge storage adapter (Psr6Storage)",
        "psr/log": "Optional structured logging of verify outcomes",
        "bel-consulting/kiwicaptcha-symfony": "Symfony bundle integration (form type, validator constraint, Twig widget, challenge endpoint)"
    },
    "autoload": {
        "psr-4": {
            "KiwiCaptcha\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "KiwiCaptcha\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "extra": {
        "branch-alias": {
            "dev-main": "1.0.x-dev"
        }
    },
    "minimum-stability": "stable"
}
